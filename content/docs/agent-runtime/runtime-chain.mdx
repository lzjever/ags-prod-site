---
title: Notebook + Files + Agents 运行链路
description: 展示用户如何在项目中完成一次通用智能体任务的统一链路。
---

<Eyebrow>Agent Runtime</Eyebrow>

# Notebook + Files + Agents 运行链路

一条成熟的通用智能体任务链路，不应该依赖临时脚本和个人目录去拼起来。更理想的方式是: 先创建 Notebook Task，再从 Files、URL 或历史 artifacts 中补充输入，随后选择 Agent 与 Endpoint，由系统完成权限与治理预检，准备 Task Workspace 与运行资产，并在执行过程中持续回传 conversation、trace 和 artifacts。任务结束后，结果会继续沉淀为项目文件和任务历史，而不是随着一次运行结束就消失。

<SiteSection
  title="为什么这条链路重要"
  description="它把运行前、运行中、运行后的关键对象统一组织到一个可回看、可复用的任务对象里。"
>

这条链路真正重要的地方，在于它把运行前、运行中和运行后的关键对象都收进了同一个任务对象里。输入不再依赖用户手工拼目录，运行过程也不再依赖终端窗口，而结果会继续留在项目上下文中，成为之后可回看、可复用、也可协作的资产。

</SiteSection>
